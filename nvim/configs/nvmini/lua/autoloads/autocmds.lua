-- vim.api.nvim_create_autocmd({ "FileType" }, {
--   group = Utils.create_augroup("leaving_cwd"),
--   callback = function()
--     if vim.bo.buftype == "" then
--       if Lonard.root() ~= vim.uv.cwd() then
--         Lonard.warn("Leaving [cwd] to " .. Utils.root(), { title = "Warning" })
--       end
--     end
--   end,
-- })
