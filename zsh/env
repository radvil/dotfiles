#!/bin/zsh

export DOTFILES="$HOME/.dotfiles"
# DO I NEED THIS ?
[[ -s "$DOTFILES/build/utils.sh" ]] && source "$DOTFILES/build/utils.sh"
[[ -d "$DOTFILES/bin" ]] && export PATH="$DOTFILES/bin:$PATH"
[[ -s "$DOTFILES/z/init.zsh" ]] && source "$DOTFILES/z/init.zsh"
